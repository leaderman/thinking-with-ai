# 微信公众号素材与草稿箱接口调研

## 官方文档地址

- 素材管理：https://developers.weixin.qq.com/doc/offiaccount/Asset_Management/New_temporary_materials.html
- 草稿箱接口：https://developers.weixin.qq.com/doc/offiaccount/Draft_Box/Add_draft.html
- 发布能力接口：https://developers.weixin.qq.com/doc/offiaccount/Draft_Box/Freepublish.html
- 第三方文档参考：https://weixingongzhonghao.com.cn/zh/api/media.html

---

## 一、素材类型说明

| 类型 | 说明 | 有效期 | 是否有公开 URL |
|------|------|------|------|
| 临时素材 | 用于发消息等短期场景 | 3 天 | 无，只能通过 media_id 使用或下载二进制 |
| 永久素材（图片） | 长期存储，上传后返回 URL | 永久（不删除则一直有效） | 有，但仅限在微信生态内使用 |

### 素材格式与大小限制

| 类型 | 格式 | 大小 | 时长 |
|------|------|------|------|
| image | JPG/PNG | ≤2MB | - |
| voice | AMR/MP3 | ≤2MB | ≤60秒 |
| video | MP4 | ≤10MB | - |
| thumb | JPG | ≤64KB | - |

永久素材数量上限：图文/图片各 5000 条，其他类型各 1000 条。

### 永久素材库的存储机制

永久素材库（media library）和实际的 CDN 存储是两层分离的：

- **素材库**：管理索引，记录哪些素材"归你管"，占用 5000 条的配额
- **CDN 存储**：图片文件的实际存放位置（`mmbiz.qpic.cn`）

删除素材库里的条目，只是移除索引、释放配额，CDN 上的图片文件本身不会被删除。因此**已发布文章中的图片，删除素材库条目后仍然正常显示**。

实际使用建议：**把素材库当临时中转站**，上传 → 拿到 `media_id` 和 `url` → 发完文章后立即删除释放配额，不影响已发布内容。

---

## 二、已废弃接口说明

`/cgi-bin/material/add_news`（上传永久图文素材）已于 **2022 年 2 月 28 日**正式废弃，原因是微信将图文素材升级为草稿箱体系。调用该接口会返回：

```json
{ "errcode": 45106, "errmsg": "This API has been unsupported." }
```

**替代方案**：改用草稿箱接口 `/cgi-bin/draft/add`。

---

## 三、实现功能清单

| 功能 | 接口 | 状态 |
|------|------|------|
| 上传本地图片为临时素材 | `POST /cgi-bin/media/upload` | ✅ 成功 |
| 上传本地图片为永久素材并获取 URL | `POST /cgi-bin/material/add_material` | ✅ 成功 |
| 获取永久素材列表 | `POST /cgi-bin/material/batchget_material` | ✅ 成功 |
| 删除永久素材 | `POST /cgi-bin/material/del_material` | ✅ 成功 |
| 新建草稿 | `POST /cgi-bin/draft/add` | ✅ 成功 |
| 获取草稿列表 | `POST /cgi-bin/draft/batchget` | ✅ 成功 |
| 删除草稿 | `POST /cgi-bin/draft/delete` | ✅ 成功 |
| 发布草稿 | `POST /cgi-bin/freepublish/submit` | ✅ 成功 |
| 查询发布状态 | `POST /cgi-bin/freepublish/get` | ✅ 成功 |
| 获取已发布文章列表 | `POST /cgi-bin/freepublish/batchget` | ✅ 成功 |
| 删除已发布文章 | `POST /cgi-bin/freepublish/delete` | ✅ 成功 |

---

## 四、关键问题与解决方案

### axios 通过代理上传 multipart 返回 503

**原因**：本机设置了 `HTTPS_PROXY=http://127.0.0.1:1234`，axios 会自动读取该环境变量并通过代理发请求，但该代理对 multipart/form-data 的 POST 请求返回 503。curl 能正常工作是因为两者实现差异，代理对 curl 的处理正常。

**解决**：在 axios 请求配置中加 `proxy: false`，绕过代理直连微信 API。

```js
axios.post(url, form, {
  headers: form.getHeaders(),
  proxy: false,
});
```

### 删除素材返回 48005

`errcode: 48005` 表示该素材被已发布文章引用，微信在索引层面加了保护，拒绝删除。但根据上文存储机制说明，这只是索引保护，CDN 文件不受影响。

### API 每日调用次数限制（45009）

`errcode: 45009` 表示当天 API 调用次数已达上限。素材管理类接口每日上限约为 1000 次，额度在北京时间每日 0 点重置。

### 查询发布状态接口注意

查询发布状态的正确接口是 `freepublish/get`，不是 `freepublish/querystatus`。

### freepublish/batchget 的查询范围限制

`freepublish/batchget` 只能查询通过 `freepublish` 体系发布的文章，以下情况查不到：

- 后台手动发布的文章
- 通过旧版群发接口（`mass/sendall`）发布的文章
- 设置为"仅自己可见"的文章

---

## 五、代码示例

### 1. 上传临时素材（图片）

**接口**：`POST https://api.weixin.qq.com/cgi-bin/media/upload?access_token=ACCESS_TOKEN&type=image`

**响应**：
```json
{
  "type": "image",
  "media_id": "MEDIA_ID",
  "created_at": 1234567890
}
```

```js
const axios = require('axios');
const FormData = require('form-data');
const fs = require('fs');
const path = require('path');

async function uploadTemporaryMedia(accessToken, filePath) {
  const form = new FormData();
  form.append('media', fs.createReadStream(filePath), {
    filename: path.basename(filePath),
    contentType: 'image/jpeg',
  });

  const url = `https://api.weixin.qq.com/cgi-bin/media/upload?access_token=${accessToken}&type=image`;
  const response = await axios.post(url, form, {
    headers: form.getHeaders(),
    proxy: false,
  });

  return response.data; // { type, media_id, created_at }
}
```

---

### 2. 上传永久图片素材（获取 URL）

**接口**：`POST https://api.weixin.qq.com/cgi-bin/material/add_material?access_token=ACCESS_TOKEN&type=image`

**响应**：
```json
{
  "media_id": "MEDIA_ID",
  "url": "http://mmbiz.qpic.cn/..."
}
```

> 注意：返回的 `url` 为微信 CDN 地址，仅限在微信图文消息正文中使用，不适合作为通用公开图片链接。

```js
const axios = require('axios');
const FormData = require('form-data');
const fs = require('fs');
const path = require('path');

async function uploadPermanentImage(accessToken, filePath) {
  const form = new FormData();
  form.append('media', fs.createReadStream(filePath), {
    filename: path.basename(filePath),
    contentType: 'image/jpeg',
  });

  const url = `https://api.weixin.qq.com/cgi-bin/material/add_material?access_token=${accessToken}&type=image`;
  const response = await axios.post(url, form, {
    headers: form.getHeaders(),
    proxy: false,
  });

  return response.data; // { media_id, url }
}
```

---

### 3. 获取永久素材列表

**接口**：`POST https://api.weixin.qq.com/cgi-bin/material/batchget_material?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | 是 | 素材类型：`image` / `video` / `voice` / `news` |
| `offset` | number | 是 | 从第几条开始，从 0 开始 |
| `count` | number | 是 | 每次拉取数量，最大 20 |

**响应**：
```json
{
  "total_count": 100,
  "item_count": 20,
  "item": [
    {
      "media_id": "MEDIA_ID",
      "name": "filename.jpg",
      "url": "http://mmbiz.qpic.cn/...",
      "update_time": 1234567890
    }
  ]
}
```

```js
async function getMaterialList(accessToken, offset = 0, count = 20) {
  const url = `https://api.weixin.qq.com/cgi-bin/material/batchget_material?access_token=${accessToken}`;
  const response = await axios.post(url, { type: 'image', offset, count }, { proxy: false });
  return response.data;
}
```

---

### 4. 删除永久素材

**接口**：`POST https://api.weixin.qq.com/cgi-bin/material/del_material?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `media_id` | string | 是 | 要删除的素材 media_id |

**响应**：
```json
{ "errcode": 0, "errmsg": "ok" }
```

```js
async function deleteMaterial(accessToken, mediaId) {
  const url = `https://api.weixin.qq.com/cgi-bin/material/del_material?access_token=${accessToken}`;
  const response = await axios.post(url, { media_id: mediaId }, { proxy: false });
  return response.data;
}
```

---

### 5. 新建草稿

**接口**：`POST https://api.weixin.qq.com/cgi-bin/draft/add?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | 是 | 文章标题 |
| `thumb_media_id` | string | 是 | 封面图的永久素材 media_id |
| `author` | string | 否 | 作者 |
| `digest` | string | 否 | 摘要，不填则取正文前 54 字 |
| `show_cover_pic` | number | 否 | 1=显示封面，0=不显示 |
| `content` | string | 是 | 正文 HTML，图片须使用微信域名 |
| `content_source_url` | string | 否 | 原文链接 |
| `need_open_comment` | number | 否 | 1=开启评论，0=关闭 |
| `only_fans_can_comment` | number | 否 | 1=仅粉丝可评论，0=所有人 |

**响应**：
```json
{
  "media_id": "MEDIA_ID",
  "item": [{ "index": 0, "ad_count": 0 }]
}
```

```js
const axios = require('axios');

async function addDraft(accessToken, article) {
  const url = `https://api.weixin.qq.com/cgi-bin/draft/add?access_token=${accessToken}`;
  const response = await axios.post(url, { articles: [article] }, { proxy: false });
  return response.data; // { media_id, item }
}

// 调用示例
addDraft(ACCESS_TOKEN, {
  title: '今天是值得纪念的一天',
  thumb_media_id: 'THUMB_MEDIA_ID',
  author: '闲人一个',
  digest: '这就是一个测试',
  show_cover_pic: 1,
  content: '<p>正文内容...</p><img src="http://mmbiz.qpic.cn/..."/>',
  content_source_url: 'https://example.com',
  need_open_comment: 0,
  only_fans_can_comment: 0,
});
```

---

### 6. 获取草稿列表

**接口**：`POST https://api.weixin.qq.com/cgi-bin/draft/batchget?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `offset` | number | 是 | 从第几条开始，从 0 开始 |
| `count` | number | 是 | 每次拉取数量，最大 20 |
| `no_content` | number | 否 | 1=不返回正文内容（减少数据量） |

**响应**：
```json
{
  "total_count": 112,
  "item_count": 20,
  "item": [
    {
      "media_id": "MEDIA_ID",
      "content": {
        "news_item": [{ "title": "文章标题", "digest": "摘要" }],
        "create_time": 1234567890,
        "update_time": 1234567890
      },
      "update_time": 1234567890
    }
  ]
}
```

```js
async function getDraftList(accessToken, offset = 0, count = 20) {
  const url = `https://api.weixin.qq.com/cgi-bin/draft/batchget?access_token=${accessToken}`;
  const response = await axios.post(url, { offset, count, no_content: 1 }, { proxy: false });
  return response.data;
}
```

---

### 7. 删除草稿

**接口**：`POST https://api.weixin.qq.com/cgi-bin/draft/delete?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `media_id` | string | 是 | 草稿的 media_id |

**响应**：
```json
{ "errcode": 0, "errmsg": "ok" }
```

```js
const axios = require('axios');

async function deleteDraft(accessToken, mediaId) {
  const url = `https://api.weixin.qq.com/cgi-bin/draft/delete?access_token=${accessToken}`;
  const response = await axios.post(url, { media_id: mediaId }, { proxy: false });
  return response.data;
}
```

---

### 8. 发布草稿

**接口**：`POST https://api.weixin.qq.com/cgi-bin/freepublish/submit?access_token=ACCESS_TOKEN`

> 注意：发布是异步的，submit 返回成功只代表任务提交，需通过 `freepublish/get` 查询最终状态。

```js
async function submitPublish(accessToken, mediaId) {
  const url = `https://api.weixin.qq.com/cgi-bin/freepublish/submit?access_token=${accessToken}`;
  const response = await axios.post(url, { media_id: mediaId }, { proxy: false });
  return response.data; // { errcode, errmsg, publish_id }
}
```

---

### 9. 查询发布状态

**接口**：`POST https://api.weixin.qq.com/cgi-bin/freepublish/get?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `publish_id` | string | 是 | 发布任务 ID，由 submit 接口返回 |

**`publish_status` 说明**：`0`=发布成功，`1`=发布中，`2`=原创失败，`3`=常规失败，`4`=平台审核不通过，`5`=用户删除，`6`=系统屏蔽

```js
async function queryPublishStatus(accessToken, publishId) {
  const url = `https://api.weixin.qq.com/cgi-bin/freepublish/get?access_token=${accessToken}`;
  const response = await axios.post(url, { publish_id: String(publishId) }, { proxy: false });
  return response.data;
  // 成功时返回：
  // {
  //   publish_status: 0,
  //   article_id: 'ARTICLE_ID',
  //   article_detail: { count: 1, item: [{ idx: 1, article_url: '文章永久链接' }] },
  //   fail_idx: []
  // }
}
```

---

### 10. 获取已发布文章列表

**接口**：`POST https://api.weixin.qq.com/cgi-bin/freepublish/batchget?access_token=ACCESS_TOKEN`

> 注意：只能查询通过 `freepublish` 体系发布的文章，后台手动发布、旧版群发、设为"仅自己可见"的文章均不在此列表中。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `offset` | number | 是 | 从第几条开始，从 0 开始 |
| `count` | number | 是 | 每次拉取数量，最大 20 |
| `no_content` | number | 否 | 1=不返回正文内容 |

```js
async function getPublishedList(accessToken, offset = 0, count = 20) {
  const url = `https://api.weixin.qq.com/cgi-bin/freepublish/batchget?access_token=${accessToken}`;
  const response = await axios.post(url, { offset, count, no_content: 1 }, { proxy: false });
  return response.data;
}
```

---

### 11. 删除已发布文章

**接口**：`POST https://api.weixin.qq.com/cgi-bin/freepublish/delete?access_token=ACCESS_TOKEN`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `article_id` | string | 是 | 已发布文章的 article_id |
| `index` | number | 是 | 文章索引，单篇传 0 |

**响应**：
```json
{ "errcode": 0, "errmsg": "ok" }
```

```js
async function deletePublishedArticle(accessToken, articleId) {
  const url = `https://api.weixin.qq.com/cgi-bin/freepublish/delete?access_token=${accessToken}`;
  const response = await axios.post(url, { article_id: articleId, index: 0 }, { proxy: false });
  return response.data;
}
```
